<!--

This FXML file defines the layout for the main view of the application using JavaFX. 
It uses a BorderPane as the root layout with a VBox on the left and a StackPane in the center.

Components:
- BorderPane: The root layout container.
   - left (VBox): Contains a vertical box with buttons for various actions.
      - Buttons:
         - importButton: Triggers the import action.
         - exportButton: Triggers the export action.
         - settingsButton: Opens the settings view.
         - aboutButton: Opens the about view.
         - exitButton: Exits the application.
      - Insets: Adds padding around the VBox.
   - center (StackPane): A placeholder for the main content area.

Attributes:
- xmlns: XML namespace for JavaFX.
- xmlns:fx: XML namespace for FXML.
- fx:controller: Specifies the controller class for handling user interactions.

Event Handlers:
- onAction: Specifies the method to call in the controller when the button is clicked.

@Author: Fatih Tolip
-->
<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.openjfx.controllers.MainController">
   <left>
      <VBox spacing="10.0" BorderPane.alignment="CENTER" alignment="TOP_CENTER" prefHeight="Infinity">
         <children>
            <Button fx:id="importButton" mnemonicParsing="false" text="%importButtoninMain" onAction="#showImport"/>
            <Button fx:id="exportButton" mnemonicParsing="false" text="%exportButtoninMain" onAction="#showExport"/>
            <Button fx:id="settingsButton" mnemonicParsing="false" text="%settingsButtoninMain" onAction="#showSettings"/>
            <Button fx:id="aboutButton" mnemonicParsing="false" text="%aboutButtoninMain" onAction="#showAbout"/>
            <Button fx:id="exitButton" mnemonicParsing="false" text="%exitButtoninMain" onAction="#exitApp"/>
         </children>
         <BorderPane.margin>
            <Insets bottom="10.0" left="10.0" right="10.0" top="10.0" />
         </BorderPane.margin>
      </VBox>
   </left>
   <center>
      <StackPane fx:id="contentPane" BorderPane.alignment="CENTER"/>
   </center>
</BorderPane>
